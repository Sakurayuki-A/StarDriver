<Window x:Class="StarDriver.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Star Driver - PSO2:NGS Downloader" 
        Height="900" 
        Width="1700"
        MinHeight="800"
        MinWidth="1200"
        Background="#0a0a0a"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="False"
        ResizeMode="CanResize">
    
    <Window.Resources>
        <!-- 侧边栏按钮样式 -->
        <Style x:Key="NavButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="6"
                                Margin="5,2">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#1a1a1a"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#151515"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 标题栏按钮样式 -->
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#ff66bb"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 关闭按钮样式 -->
        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#e81123"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32"
                      ResizeBorderThickness="5"
                      GlassFrameThickness="0"
                      CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 自定义标题栏 -->
        <Border Grid.Row="0" 
                Background="#ff44aa"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 应用图标和标题 -->
                <StackPanel Grid.Column="0" 
                            Orientation="Horizontal" 
                            Margin="10,0,0,0"
                            VerticalAlignment="Center">
                    <TextBlock Text="⭐" 
                               FontSize="16" 
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>
                    <TextBlock Text="Star Driver - PSO2:NGS Downloader" 
                               FontSize="12" 
                               Foreground="White"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- 窗口控制按钮 -->
                <StackPanel Grid.Column="2" 
                            Orientation="Horizontal"
                            WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button Content="─" 
                            Style="{StaticResource TitleBarButtonStyle}"
                            Click="MinimizeButton_Click"/>
                    <Button x:Name="MaximizeButton"
                            Content="□" 
                            Style="{StaticResource TitleBarButtonStyle}"
                            Click="MaximizeButton_Click"/>
                    <Button Content="✕" 
                            Style="{StaticResource CloseButtonStyle}"
                            Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主内容区域 -->
        <Grid Grid.Row="1">
            <!-- 页面内容容器（占满整个区域，左侧留出侧边栏空间） -->
            <Border x:Name="ContentBorder"
                    Background="#0a0a0a" 
                    Padding="230,30,30,30">
                <ContentControl x:Name="PageContent"/>
            </Border>
            
            <!-- 通知容器（右上角） -->
            <StackPanel x:Name="ToastContainer"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0,20,20,0"
                        Width="350"
                        Panel.ZIndex="5"/>

            <!-- 侧边栏（覆盖在内容上方，固定宽度 200px） -->
            <Border x:Name="SidebarBorder"
                    Width="200"
                    HorizontalAlignment="Left"
                    Panel.ZIndex="10"
                    Background="#0f0f0f" 
                    BorderBrush="#1a1a1a" 
                    BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Logo -->
                    <StackPanel Grid.Row="0" Margin="20,30,20,30">
                        <Grid>
                            <!-- 主标题文字 -->
                            <TextBlock Text="Star Driver" 
                                       FontSize="18" 
                                       FontWeight="Bold" 
                                       Foreground="#666666"/>
                            
                            <!-- 流光效果层 -->
                            <TextBlock Text="Star Driver" 
                                       FontSize="18" 
                                       FontWeight="Bold">
                                <TextBlock.Foreground>
                                    <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                        <LinearGradientBrush.GradientStops>
                                            <GradientStop Color="Transparent" Offset="0"/>
                                            <GradientStop Color="Transparent" Offset="0.3"/>
                                            <GradientStop Color="#60C0C0C0" Offset="0.45"/>
                                            <GradientStop Color="#CCFFFFFF" Offset="0.5"/>
                                            <GradientStop Color="#60C0C0C0" Offset="0.55"/>
                                            <GradientStop Color="Transparent" Offset="0.7"/>
                                            <GradientStop Color="Transparent" Offset="1"/>
                                        </LinearGradientBrush.GradientStops>
                                        <LinearGradientBrush.Transform>
                                            <TranslateTransform x:Name="LogoShineTransform" X="-300"/>
                                        </LinearGradientBrush.Transform>
                                    </LinearGradientBrush>
                                </TextBlock.Foreground>
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="White" 
                                                      BlurRadius="10" 
                                                      ShadowDepth="0" 
                                                      Opacity="0"/>
                                </TextBlock.Effect>
                                <TextBlock.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard RepeatBehavior="Forever">
                                                <DoubleAnimation Storyboard.TargetName="LogoShineTransform"
                                                                 Storyboard.TargetProperty="X"
                                                                 From="-300"
                                                                 To="300"
                                                                 Duration="0:0:2.5"
                                                                 BeginTime="0:0:0"/>
                                                <DoubleAnimation Storyboard.TargetProperty="(TextBlock.Effect).(DropShadowEffect.Opacity)"
                                                                 From="0"
                                                                 To="0.6"
                                                                 Duration="0:0:0.3"
                                                                 AutoReverse="True"
                                                                 BeginTime="0:0:1.25"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </TextBlock.Triggers>
                            </TextBlock>
                        </Grid>
                    </StackPanel>

                    <!-- 导航菜单 -->
                    <StackPanel Grid.Row="1" Margin="10,0">
                        <RadioButton x:Name="HomeNavButton"
                                     Content="{DynamicResource Nav.Home}" 
                                     Style="{StaticResource NavButtonStyle}"
                                     Checked="HomeNavButton_Checked"/>
                        
                        <RadioButton x:Name="DownloadsNavButton"
                                     Content="{DynamicResource Nav.Downloads}" 
                                     Style="{StaticResource NavButtonStyle}"
                                     Checked="DownloadsNavButton_Checked"/>
                        
                        <RadioButton x:Name="SettingsNavButton"
                                     Content="{DynamicResource Nav.Settings}" 
                                     Style="{StaticResource NavButtonStyle}"
                                     Checked="SettingsNavButton_Checked"/>
                    </StackPanel>

                    <!-- 底部品牌 -->
                    <StackPanel Grid.Row="2" Margin="20,20">
                        <TextBlock Text="NEXUS" 
                                   FontSize="12" 
                                   FontWeight="Bold" 
                                   Foreground="#222222" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="HYPER TRANSFER" 
                                   FontSize="8" 
                                   Foreground="#1a1a1a" 
                                   HorizontalAlignment="Center"
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
